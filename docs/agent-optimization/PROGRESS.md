# agent 功能优化 · 进度

## 基线

- 工作树：`/home/xixi/Git/OmniStudio/.claude/worktrees/agent-optimize`，分支 `agent-optimize`
- 起点提交：`24199d4`（origin/main，v0.1.4）—— 清单里所有 file:line 只对这个提交成立
- 依赖已装（bun 1.4.2，964 包）
- **绿线基线（2026-09-21）**：`bun run lint` 通过；`bun run typecheck` 通过；`bun run test` 2267 通过 / 3 跳过 / **0 失败**，用时 10.6 秒
- 已知红：无
- **已知抖动**（都是并行负载下的计时敏感用例，单独跑必绿，与改动无关；验收时重跑一次即可，不要算到 pi 头上）：
  - `apps/studio/src/bun/memory.test.ts:354`，断言 `elapsed < 2000` 毫秒，实测偶尔到 2900 毫秒。
  - `apps/studio/src/bun/agent-hooks.test.ts`，「stdout 不是 JSON → 整段当上下文」那条，钩子要起子进程、等超时，负载高时会超。单独连跑 5 次全绿。
  - 两个文件都不 import `agent-tools`（`memory.ts` 只 `import type`，运行时被擦除），所以结构上就不可能被前两条改动影响。
  - 全量跑一次大约出 1 条抖动，重跑基本不会连中同一条。做第 07 条（钩子）时要特别注意第二条抖动，别把它当成自己改坏的。
- 注意：测试必须用 `bun run test`（内部是 `bun test --parallel`）。在仓库根直接敲 `bun test` 会因为 mock 串扰出现约 148 个假失败，那是调用方式错，不是代码问题。
- `test:smoke` 不进验收门：里面有要连真实服务和密钥的脚本，本机跑不过。

## 流水线

- 规则：`docs/agent-optimization/PI-RULES.md`（每次派发都随任务一起给 pi）
- 验收门：`docs/agent-optimization/gate.sh`，manifest 在 `manifests/`
  - 派发前：`gate.sh <manifest> --baseline` 抓用例名单
  - pi 自检：`gate.sh <manifest>`
  - 主 agent 验收：`gate.sh <manifest> --full`（多跑一项「源码退回 HEAD 后测试必须变红」）
- 派发形态：`pi -p --thinking low --no-skills --session-id <id> @PI-RULES.md @tasks/<id>.md "一句话指令"`，后台跑、日志落 `logs/`，超时 25 分钟
- 串行，一条过一条提交。并行会让越界检查互相污染。

## 条目状态

清单全文见 `SUMMARY.md`（37 条，五批）。复核状态沿用那里的标注：已复核 / 已复现 / 报告。

| # | 批次 | 标题 | 复核 | 状态 | session-id | 提交 |
|---|---|---|---|---|---|---|
| 01 | 一 | `edit_file` 的 `$` 替换序列 + 空 old_str | 已复核 | **已验收** | `omni-agent-01` | 见下 |
| 02a | 一 | 沙箱用 tmpfs 遮盖凭据文件导致 bwrap 启动失败 | 已复现 | **已验收** | `omni-agent-02a` | 见下 |
| 02b | 一 | 文件类凭据的覆盖源换成真实空文件（现在是 /dev/null，读到的是权限拒绝） | 已复现 | 待派发 | — | — |
| 03 | 一 | 排队消息写库两次 | 已复核 | 待派发 | — | — |
| 07a | 一 | 钩子负载走环境变量触发 E2BIG | 已复核 | **已验收** | `omni-agent-07a` | 见下 |
| 07b | 一 | ~~拦截型事件的钩子启动失败应当拦截而非放行~~ | 已复核 | **撤销，见下** | — | — |
| 08 | 一 | glob 的 `**/` 不匹配零层目录 | 已复核 | **已验收** | `omni-agent-08` | 见下 |
| 04 | 一 | 会话重入闸门 | 已复核 | 待派发 | — | — |
| 05 | 一 | 停止信号传不到子智能体 | 部分复核 | 待派发 | — | — |
| 06 | 一 | 失败回合返回成功 | 已复核 | 待派发 | — | — |
| 09 | 一 | 三处界面入口失效 | 部分复核 | 待派发 | — | — |
| 10 | 一 | 退出不收尾 agent | 已复核 | 待派发 | — | — |
| 11 | 二 | Goal 模式进度行写进系统提示，前缀缓存每轮作废 | 已复核 | **已验收** | `omni-agent-11` | 见下 |
| 12 | 二 | 裁剪只钉第一条，长回合裁掉当前轮的任务陈述 | 已复核 | **已验收** | `omni-agent-12` | 见下 |
| 13–19 | 二 | 上下文质量与 token 成本（其余） | 混合 | 待派发 | — | — |
| 20–25 | 三 | 界面流畅度 | 混合 | 待派发 | — | — |
| 26–29 | 四 | 安全加固 | 报告 | **阻塞：等产品决策** | — | — |
| 30–37 | 五 | 结构与测试 | 报告 | 暂缓 | — | — |

## 验收记录

### 01 · `edit_file`（通过）

- pi 改动落在允许的两个文件内，源码净增 1 行（3 增 3 删）：`agent-tools.ts:558` 加空串守卫，`:567-573` 单次替换改成 indexOf + slice 拼接，`replace_all` 的 split/join 路径原样未动。
- 验收门 `--full` 全项通过，含「源码退回 HEAD 后测试变红」与还原校验和一致。
- **定点变异抽查（主 agent 独立做，门不覆盖）**：只把单次替换那一处改回 `String.replace`、保留空串守卫，结果恰好是第 1、2 条用例变红、其余 3 条绿。说明用例真的咬住了这处改动，不是空转。
- 全量 `bun run test`：2272 通过 / 0 失败（基线 2267，新增 5 条）。
- pi 的自报与实测一致，本次没有发现虚报。

### 08 · `glob` 的 `**/`（一次返工后通过）

- pi 把 `**/` 整体译成可选目录前缀 `(?:.*/)?`，只动 `globToRegExp` 内部，11 行。写法是对的。
- **第一轮验收打回**：6 条用例全绿、门 `--full` 也全过，但我做定点变异——把 `(?:.*/)?` 换成朴素的 `.*`——6 条**仍然全绿**。说明用例分不出正确写法和近似写法。
  - 两者的真实差别：`(?:.*/)?` 要求前缀在斜杠处结束，`.*` 不要求。实测 `**/b.ts` 配 `src/ab.ts`，pi 的写法判不匹配（对），朴素写法判匹配（错）；`**/x/*.ts` 配 `ax/c.ts` 同理。
- 用同一个 session-id 续接返工，只许加测试、不许动源码。补了 2 条边界用例，源码 numstat 确认没动（仍是 9 增 2 删）。
- 返工后门 `--full` 全过，其中新加的自动变异项报 PASS（变异后测试变红）。
- 全量测试 2279 通过，另有 1 条已知抖动，与本条无关。

**由此给验收门加了 `mutations` 配置项**：manifest 里写「把哪段换成哪段近似写法」，`--full` 时自动跑、测试必须变红。整体回退验红只能证明「测试咬住了这次改动」，抓不住「测试分不清正确写法和近似写法」——这一层正好补上。后续每条都要配。

### 02a · 沙箱凭据文件（一次超时后拆成两趟，通过）

- **第一趟超时**（`pi_exit=124`，25 分钟磨满）。日志里零个 `GATE:`、零个汇报标题，按判据算输出崩坏；但查工作区发现测试**已经写完而且写得对**（3 条用例，按「开关 + 紧跟参数」做配对断言），只是没来得及动源码。
  - 原因：前两条成功的任务都是**新建**测试文件，这条要改既有的 `agent-sandbox.test.ts`（801 行 / 39 条用例），源码 `agent-sandbox.ts` 又是 790 行，加起来近 1600 行，光读就耗掉大半时间。
- **第二趟**保留测试、同 session-id 续接，任务收窄到「只改源码那两处」，连要加的 `blankFile?: string;` 和它在参数列表里的位置都直接写给它，红的输出也原样贴进说明。15 分钟内交付，`pi_exit=0`。
- 源码 18 行：`bwrapArgs` 加可选 `blankFile`（默认 `/dev/null`），凭据路径按 `statSync().isDirectory()` 分流——目录仍 `--tmpfs`，文件改 `--ro-bind <blankFile> <target>`，`statSync` 包了 try/catch，异常按文件处理。
- 门 `--full` 全过，含自动变异项（把目录/文件的区分去掉后测试变红）。用例 39 → 42，一条没丢。
- **真实 bwrap 端到端验证（主 agent 独立做，门只校 argv 形状）**：假家目录里放 `.npmrc` 和 `.ssh`，用生成的 argv 真跑一次 bwrap——退出码 0（修之前这里是 1，报 `Can't mkdir …: Not a directory`），且沙箱内读 `.npmrc` 拿到「权限拒绝」，凭据内容读不到。两个性质都成立。
- 全量测试 2282 通过，另有 1 条已知抖动。

**流水线经验（后续条目必须照办）**：凡是要改**既有大测试文件**的条目，一律拆成「写测试」和「改源码」两趟派发，不要指望一趟过。第 07（钩子）、第 04（会话重入）、第 06（回合返回值）都会撞上同样情况。

### 07a · 钩子负载走环境变量（通过，但返工那一轮是主 agent 判断失误）

- 派发前先实测了阈值：单个环境变量 127KB 能起进程，128KB 抛 `E2BIG`；用异步 `Bun.spawn` 也复现了同样行为，确认 `agent-hooks.ts:144` 的 spawn 确实会掉进 `:160` 的 catch，返回 `blocked: false`——拦截型钩子被静默绕过。
- 源码 7 行：加 `MAX_PAYLOAD_ENV_BYTES = 64 * 1024`，用条件展开决定要不要设 `OMNI_HOOK_PAYLOAD`，并总是给 `OMNI_HOOK_PAYLOAD_BYTES`。stdin 那一路没动。
- 用的是 `Buffer.byteLength(serialized, "utf8")`。manifest 里把「换成 `.length`」配成自动变异项，门实测能抓住。pi 还自己多加了第 4 条中文负载用例。
- pi 顺手把一条既有用例的注释里直引号改成了弯引号（违反「现有用例一个字不许动」），主 agent 已改回，测试文件最终是 83 增 0 删的纯新增。
- 门 `--full` 全项通过；全量测试 2287 通过 0 失败。

**返工那一轮是白跑的，起因是主 agent 自己的核查错误。** 记录下来避免重犯：

- 我判定「这 4 条新用例引入了仓库里前所未有的 python3 依赖」，据此打回返工。
- 实际情况：`agent-hooks.test.ts` 在 HEAD 上**本来就有 2 处 python3**（第 102 行和第 356 行，都在既有用例里），全仓库还有另外 4 个测试文件也用 python3。
- 出错原因：我跑的是 `grep -rln python3 <路径> --include=*.test.ts`，zsh 对 `--include=*.test.ts` 做了文件名展开、没匹配上就整条命令报错退出（`no matches found`），我把**空输出当成了「没有匹配」**，而不是「命令根本没跑」。
- 教训：zsh 下不要给 grep 传 `--include=*.xxx` 这类带通配的参数；要过滤扩展名就先 `grep -rln` 再管道过滤文件名。**空输出必须先确认命令真的跑成功了**，尤其是退出码非 0 的时候。
- 返工结果本身无害（新用例改用纯 shell，也能跑），所以保留，没有回退。

### 07b · 撤销，不作为缺陷修

原本要把「钩子启动失败时放行」改成「拦截」。复核后撤销，两条理由：

1. **失败不阻断是写进契约的刻意设计**，不是疏漏。`agent-hooks.ts:18` 的模块注释原话：「**失败不阻断**：非零退出 / 超时 / 输出乱码都只记一条警告（`decision: block` 才是明确的拦）」。翻转它等于改产品行为，不是修 bug。
2. **失败也不是「静默」的**，我原先的描述夸大了。`agent-hooks.ts:294-300` 会记一条 warn 级日志，`agent.ts:2155-2164`（session_start）和 `:2736-2744`（user_prompt_submit）还会把「失败 N 条：<原因>」作为 status 事件写进会话轨迹，用户在界面上看得到。

真要让安全敏感的用户能选「失败即拦」，合理形态是给单条 hook 加一个 `failClosed` opt-in 配置，默认仍是放行。那属于新功能，和第四批那两个产品决策一起等用户拍板，不塞进缺陷修复批次。

07a 修掉的那个才是真缺陷：那不是「钩子失败了」，是**钩子根本没跑起来**，而且用户配的拦截逻辑一次都没执行过。

### 11 · Goal 模式前缀缓存（一趟通过）

- 源码只有 2 行：`agent-goals.ts:209` 的 `**进度**` 行从 `goalPromptSection` 删掉，同一份内容加进 `goalContinuationText`（续跑时当用户消息推，位置靠后，不影响前缀缓存）。`describeGoal` 没动，`goal` 工具的 get 操作照样看得到进度。
- 门 `--full` 全项通过，含自动变异项。用例 16 → 19，一条没丢。全量 2289 通过，另有 1 条已知抖动。
- **端到端复核（主 agent 独立做）**：`buildSystemPrompt` 是 `agent.ts:718` 的模块私有函数，探针导不进来，改为通读它到 `:800`。除目标段落外，它的输入只有项目指令（AGENTS.md）、技能清单、常驻记忆三项，都是「用户不改就不变」的，函数体里没有 `Date` / `now` / `random` 一类易变来源。所以进度行拿掉之后，Goal 模式下整份系统提示在相邻两轮之间确实逐字节相同，`agent.ts:2716` 那个「变了才写回」的判断不会再每轮命中。

### 12 · 钉住当前轮的任务陈述（一次返工后通过）

缺陷：`agent-compaction.ts:40` 的注释写着「第一条消息一定是任务陈述」，这个前提只在单轮会话里成立。调用点 `agent.ts:2042-2048` 传进来的是整个会话历史，多轮时 `messages[0]` 是**第一轮**的问题；回合一长，从后往前的循环走不到当前轮的 user 消息就 break，模型于是看得到旧任务、看得到最近的工具结果，**唯独看不到这一轮要干什么**。

改法：额外钉住最后一条 user 消息，开销提前计入预算；**先剥尾部悬空工具结果、再补插任务陈述**（顺序反了的话，紧跟其后的那条工具结果检查不到，其 tool_call 已被裁掉，真实 OpenAI 兼容服务会 400）。

**第一轮交付门全绿但有真 bug，主 agent 读 diff 时发现并用探针坐实：**

- `taskIndex` 那个分支 `continue` 前没更新 `firstKept`，循环走过它再 break 时，末尾的补插判断仍然成立 → **同一条任务陈述出现两次**，`dropped` 计数也跟着偏小。
- 探针实测：同一组消息扫 7 档预算，60/80/100/120/150 都出现 2 次，200/300 正常。触发条件是「循环刚好放得下任务陈述、下一条大填充放不下就 break」，pi 自己那 3 条用例的预算恰好都不在这个区间。
- 返工把 `firstKept` 的更新挪到两个分支共用的位置，并补了一条用例钉住「只出现一次」。
- **返工后全参数空间扫描**（填充条数 × 尾部角色 × 尾部条数 × 预算 20–400，共 1872 种组合）：任务陈述恰好出现一次、占位之后不出现悬空工具结果、`dropped` 计数一致，**异常 0**。
- 门 `--full` 全过，用例 22 → 26；全量 2294 通过 0 失败。

**流水线补一条规矩**：变异检查只能验证「测试钉住了我预想的那处语义」，验不出「实现里新引入的状态不一致」。**凡是改动引入了新的可变状态（计数器、下标、标志位），验收时必须写探针扫参数空间**，不能只看门的结果。前面几条都是纯函数式替换（换挂载参数、删一行、换判断依据），没这个风险；12 是第一条引入循环内状态的。

**对 pi 的判断要修正一条**：到目前为止它没有谎报过结果，虚报的是**覆盖度**（以为测到了），不是**结论**（没谎称通过）。验收重点应放在「测试够不够」而非「它说的是不是真的」。

## 阻塞与决策

1. **第四批阻塞**，两个问题要用户定：
   - 沙箱不可用时，是拒绝执行，还是明确告警后放行？
   - 授权粒度要不要拆成「读」和「写」两种？
2. **第四、五批多数条目标的是「报告」（未复核）**，按验证规则，要先由主 agent 对着真实文件复核一遍，才能写成 pi 的任务说明。这部分不外包。
3. **第五批里的跨模块重构**（670 行函数拆分、rpc 段抽离）不排进前几波：pi 实测在这类任务上会磨到超时。

## 派发观察项

- ~~仓库根 `AGENTS.md` 有 40,968 字节，会不会把任务说明冲淡~~ —— 第 01 条实测**没有影响**：日志从第 2 行就直接进入交付汇报，六节结构完整、全部在题。`pi -p --no-skills` 加任务说明的形态可以照用。

## 顺序调整

**第 03 条（排队消息写库两次）从第一波挪后**，原因是它测不动，不是难改：

- 改法本身只有一行：`agent.ts:3185` 的 `runAgentTurn` 调用补上 `insertUserMessage: false`。同一文件 `:3267-3270` 的重新生成路径已经是这么写的，`:3264-3266` 的注释把道理讲得很清楚——不传就会多出一个一模一样的用户气泡，模型在历史和 prompt 里各看到一遍。
- 但要证明它，得真跑完一个排队回合。而 `runAgentTurn` 在 `:2413-2439` 没有模型或推理服务地址时就早返回了，早于 `:2465` 那次插库，缺陷根本走不到。真做出来要桩一个推理服务——那正是清单第 36 条的工作（把桩模型从冒烟脚本抽成测试工具）。
- 让 pi 顺手搭这套桩，正好踩中它「倾向过度打桩」和「任务一大就崩」两个毛病。
- 所以：先做有干净单测面的条目（08、02、07），等桩模型工具就位后再回头做 03。

第一波调整后的顺序：**01 ✅ → 08 → 02 → 07a → 07b → 03**。
